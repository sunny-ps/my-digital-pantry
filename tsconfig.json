{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "$components/*": ["app/components/*"],
      "$hooks": ["app/hooks"],
      "$types": ["app/types"]
    }
  }
}
